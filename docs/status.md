# 开发状态

记录日期：2026-09-18。当前版本为 0.1.0 开发预览，完成 D1 及部分 D2 的纵向实现，不是完整 P0 交付。

## 已实现

- 独立项目、Git、本地构建、F5 启动、VSIX 打包；没有配置远端或推送。
- 自定义编辑器、活动栏笔记列表、Markdown/GFM 与安全 HTML 展示。
- MathJax 基础公式及 Mermaid 本地资源渲染。
- 全文/块级源码草稿与应用、文本范围校验、版本冲突拒绝、保存对接。
- 普通矩形 Markdown/HTML 表格单元格片段、增删行列、行列移动和指针拖动。
- Markdown 对齐随列移动，HTML 属性和富文本原始内容随单元格移动，表格之外的源码保持不变。

## 已执行验证

- TypeScript 类型检查及 esbuild 构建通过。
- 10 项核心测试通过：混排与公式、安全 HTML、Markdown/HTML 重排保真、行列增删、管线符、复杂表格保护、无效范围、UTF-16/CRLF。
- Browser 本地预览：Markdown 列工具栏移动、指针拖动行后顺序改变、双击单元格编辑并应用；公式与 Mermaid 出现渲染结果，浏览器错误日志未发现错误。
- npm 生产依赖审计为 0 个已报告漏洞；使用锁定覆盖修复 lodash-es 与 xmldom 间接依赖。
- 已生成本地 VSIX；包含运行时代码、离线图形模块和第三方许可声明，不包含 node_modules、测试配置、开发缓存或 Git 数据。

宿主集成测试情况将在本轮运行结束后更新。单元测试和浏览器内存预览不代表 VS Code 宿主的保存、撤销和多视图已经通过测试。

## 当前实现边界

1. 可视化正文连续输入还未实现，目前通过渲染块旁的“编辑”进入源码片段。单元格显示原始片段，完整行内富文本编辑尚待实现。
2. 图谱、反向链接、Wiki 链接、标题/块引用、嵌入及完整语法矩阵仍待开发。
3. HTML 表格仅处理可定位的普通矩形结构；colgroup、省略闭合标签、混合 HTML 块内表格等仍需扩展。
4. 基础拖动已实现，复杂自动滚动、窗口失焦取消、焦点恢复及全部拖动边界还需系统验收。
5. 未应用的草稿不是 VS Code 文本文档内容；关闭/恢复、自动保存与无闪烁撤销需要进一步设计及真实宿主验证。
6. 公式和 Mermaid 基础渲染不等同于全部稳定图类型/数学语法验证。未执行大库、100×20 表格和完整 IME/可访问性验收。
7. 目前仅有笔记文件列表，列表上限 2,000 项；正式索引、取消和增量图谱尚待实现。

## 接下来

- 完成真实扩展宿主保存、撤销、多视图和冲突测试。
- 完成 D2 的连续编辑与保真设计，再补齐完整 Markdown/笔记语法。
- 接入链接索引、块引用、笔记嵌入和图谱。
- 扩展表格表现、移动交互和性能；按需求逐条执行 A01～A23。
- GitHub 远端由用户处理，Marketplace 发布继续延期。

需求矩阵仍以 `requirements.md` 为准，以上未完成项没有从首版范围移除。
